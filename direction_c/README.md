# Direction C

This folder contains the active Direction C planning documents, execution scripts, and protocol notes for the LLM-representation track.

- Planning and strategy material lives here instead of the workspace root.
- Execute Direction C scripts from the workspace root with paths like `python direction_c/direction_c_compute_embeddings.py`.
- Generated outputs still appear in the workspace-level `embeddings/`, `feature_matrices/`, `phase3_results/`, and `phase4_results/` directories.