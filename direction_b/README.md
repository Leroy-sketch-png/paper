# Direction B

This folder contains the active federated pretraining framework material.

- `federated_pretraining_framework.py` is the current Direction B protocol and overhead-estimation entry point.
- Run it from the workspace root with `python direction_b/federated_pretraining_framework.py`.