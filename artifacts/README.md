# Artifacts

This folder holds verified downloaded artifacts and regenerated result files that do not need to sit at the workspace root.

- `falcon.zip` is the downloaded Zenodo artifact bundle.
- `falcon_comprehensive_results_all_projects.csv` is the extracted FALCON summary used by local analysis scripts.
- `heuristic_baselines.csv` is the regenerated FFF and Random-30 baseline summary.

Prefer regenerating derived files instead of hand-editing them.