# Direction A

This folder contains the active flaky-label and flaky-test protocol material.

- `flaky_detection_protocol.py` is the current Direction A framework and simulation entry point.
- Run it from the workspace root with `python direction_a/flaky_detection_protocol.py`.