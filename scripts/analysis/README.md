# Analysis Scripts

This folder contains the real implementations for the active analysis helpers:

- `bootstrap_ci.py`
- `heuristic_baselines.py`
- `print_results.py`
- `comprehensive_results.py`

The workspace root keeps thin wrappers with the same names so existing commands and document references continue to work.